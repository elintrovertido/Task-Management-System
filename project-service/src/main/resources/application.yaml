spring:
  application:
    name: project-service

  data:
    mongodb:
      host: localhost
      port: 27017
      database: project-service

    redis:
      host: localhost
      port: 6379

server:
  port: 8082
  servlet:
    context-path: /api/projects