spring:
  application:
    name: user-service

  data:
    mongodb:
      host: localhost
      port: 27017
      database: user-service

    redis:
      host: localhost
      port: 6379

server:
  port: 8081
  servlet:
    context-path: /api/users